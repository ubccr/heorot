## Required keys:

```bash
# bmc.key - needed for idrac console redirection
# Can be generated with
$ ssh-keygen

# server.cert & server.key - needed for https connection
# Can be generated with
$ openssl
```
